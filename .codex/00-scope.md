## Scope & priorities

- Project: describe_me (snapshot/monitoring + web UI + SSE + plugins/SDK).
- Priorities: security > reliability > maintainability > performance.
- Prefer deterministic logic in Rust; LLM is narration/correlation, not computation.
- Avoid breaking public API; minimize behavior changes without tests.
