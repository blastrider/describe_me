## Résumé

Expliquez brièvement le changement et les impacts utilisateurs/docs.

## Checklist

- [ ] J'ai exécuté `make ci` (ou les commandes ciblées) et vérifié l'absence de régressions.
- [ ] Les artefacts `.deb` continuent de se construire (job GitHub Actions "Build Debian package").
- [ ] Si cette PR prépare une release : `make release-<patch|minor|major>` (ou dry-run) a été lancé, et `CHANGELOG.md` est à jour.
- [ ] La branche `integration` est prête à être fusionnée vers `main` une fois la PR validée.

## Notes additionnelles

Ajoutez ici les éléments qui aident la relecture (tests manuels, captures, risques connus...).
