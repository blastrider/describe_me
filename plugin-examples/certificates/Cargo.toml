[package]
name = "describe-me-plugin-certificates"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
describe_me_plugin_sdk = { path = "../../describe_me_plugin_sdk" }
serde_json = "1.0"
x509-parser = "0.16"
rustls-pemfile = "1.0"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
